#!/usr/bin/env zx

$`git config --local core.autocrlf false`
$`git config --local http.proxy ""`
$`git config --local http.sslcainfo ""`
$`git config --local http.sslBackend "gnutls"`
$`git config --local http.sslVerify false`
